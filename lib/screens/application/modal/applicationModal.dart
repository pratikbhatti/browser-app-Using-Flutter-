class SocialModal {
  String? image, name;
  SocialModal({this.image, this.name});
}

class MusicModal {
  String? image, name;
  MusicModal({this.image, this.name});
}

class NewsModal {
  String? image, name;
  NewsModal({this.image, this.name});
}

class OnlineModal {
  String? image, name;
  OnlineModal({this.image, this.name});
}
